{
    "version": "2.0.0",
    "fields": [
      {
        "field": "USERNAME",
        "displayName": "ServiceNow Username",
        "type": "String",
        "minLength": 4,
        "maxLength": 40,
        "required": true,
        "description": "ServiceNow Instance Username",
        "example": "admin",
        "helpSection": "The username associated with your ServiceNow instance"
      },
      {
        "field": "PASSWORD",
        "displayName": "ServiceNow Password",
        "type": "String",
        "minLength": 4,
        "maxLength": 40,
        "required": true,
        "description": "ServiceNow Instance Password",
        "example": "9tGk!f3dBpJL",
        "helpSection": "The password associated with your ServiceNow instance"
      },
      {
        "field": "AUTH_TYPE",
        "displayName": "ServiceNow Auth Type",
        "type": "String",
        "minLength": 4,
        "maxLength": 40,
        "required": true,
        "description": "ServiceNow Auth Type",
        "example": "BASIC_AUTH",
        "default": "BASIC_AUTH"
      },
      {
        "field": "SERVER_URL",
        "displayName": "ServiceNow Server URL",
        "type": "String",
        "minLength": 4,
        "maxLength": 40,
        "required": true,
        "description": "ServiceNow Server URL",
        "example": "https://dev102744.service-now.com",
        "helpSection": "The name assigned to your ServiceNow instance"
      }
    ]
  }
